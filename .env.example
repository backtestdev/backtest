# OpenAI API key for natural language strategy parsing
# Get one at https://platform.openai.com/api-keys
# Optional: the app will use rule-based parsing as fallback if not set
OPENAI_API_KEY=sk-your-key-here
